# .env.example

# -- Keycloak Client Secret --
# Obtain from the Keycloak setup documentation 
KEYCLOAK_BACKEND_CLIENT_SECRET="YOUR_BACKEND_CLIENT_SECRET_HERE"

# Other configuration variables (like issuer URI, audience, service URLs, data path)
# are set directly in the docker-compose.yml file for this project.